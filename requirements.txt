# requirements.txt
# Complete dependencies for FLUX Training GUI
# Install with: pip install -r requirements.txt

# PyTorch (install separately with CUDA support first)
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# GUI
PyQt6>=6.6.0
matplotlib>=3.8.0

# Core ML Libraries
diffusers>=0.30.0
transformers>=4.43.0
accelerate>=0.32.0
safetensors>=0.4.0
sentencepiece>=0.1.99
protobuf>=4.25.0

# Image Processing
opencv-python==4.10.0.84
numpy==1.26.4
Pillow>=10.0.0
imageio>=2.31.0
imageio-ffmpeg>=0.4.9

# ai-toolkit Dependencies
albumentations>=1.3.1
lycoris_lora>=3.0.0
prodigyopt>=1.0
controlnet_aux>=0.0.7
peft>=0.7.0
bitsandbytes>=0.41.0
omegaconf>=2.3.0
pytorch-lightning>=2.1.0
einops>=0.7.0
timm>=0.9.12
kornia>=0.7.0
open-clip-torch>=2.23.0
lpips>=0.1.4
invisible-watermark>=0.2.0
torchmetrics>=1.2.0
ftfy>=6.1.3
regex>=2023.10.3
tqdm>=4.66.0

# Configuration & Utilities
pyyaml>=6.0
toml>=0.10.2
huggingface-hub>=0.20.0
tokenizers>=0.15.0

# Video/media (required by ai-toolkit import chain)
av>=12.0.0

# Optional but recommended
wandb>=0.16.0
tensorboard>=2.15.0
